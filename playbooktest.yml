---
- hosts: all
  collections:
    - ansible.windows

  tasks:
    - name: Remove obsolete shortcut
      win_file:
        path: C:\Users\%USERNAME%\Desktop\obsoleteShortcut.exe
        state: absent
