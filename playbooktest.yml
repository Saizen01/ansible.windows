---
- hosts: all
  collections:
    - ansible.windows

  tasks:
    - name: Remove obsolete shortcut
      ansible.windows.win_file:
        path: C:\Users\usr-nagios\Desktop\obsoleteShortcut.exe
        state: absent
